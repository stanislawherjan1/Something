{
  "name": "email-mcp",
  "version": "1.1.0",
  "description": "Multi-account IMAP MCP server with read + write (send / reply / forward / archive / move / mark / delete / draft). Read tools are unrestricted; every write goes through the email-write-protocol skill which forces explicit confirmation before any mutation.",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "imapflow": "^1.0.183",
    "mailparser": "^3.7.1",
    "nodemailer": "^8.0.7"
  },
  "engines": {
    "node": ">=18"
  }
}
